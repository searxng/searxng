# This SearXNG setup is used in unit tests

use_default_settings:

  engines:
    # remove all engines
    keep_only: []

engines:

  - name: nvd
    engine: nvd
    categories: ["it"]
    shortcut: "nvd"
    timeout: 9.0
    disabled: true
